---
name: dark-green-deck
description: 深色商务绿风格的飞书幻灯片制作技能。深藏青底 + 亮绿主色 + 明黄点缀的现代科技风，内置封面、数据概览、柱状图、左图右文、卡片拼贴、三栏、时间线、价值块、团队页、结束页等通用版式。当用户要求制作商务汇报、工作总结、年度复盘、项目汇报、方案演示类 PPT，且希望风格统一为深色科技/商务风时使用。支持任意页数，根据内容自动选择版式组合，不写死固定页数。
---

# Dark Green Deck

一套深色商务绿风格的飞书幻灯片制作流程。视觉系统固定，版式可按内容自由组合。

## 何时触发

- 用户要做商务汇报、工作总结、年度复盘、项目汇报、方案演示等 PPT
- 偏好深色底 + 绿色主色的现代科技/商务风格
- 给了文字/文档内容，要求排成 PPT
- 不触发：用户明确指定其他配色、或要求浅色/卡通/国风等其他风格

## 核心流程

### 1. 读内容，规划页数和版式

**不要写死页数**。根据用户内容量决定：
- 短汇报（3–5 页）：封面 + 1–3 个内容页 + 结束页
- 标准汇报（6–12 页）：封面 + 目录/概览 + 3–8 个内容页 + 结束页
- 长汇报（12 页以上）：增加章节过渡页，内容页可重复使用同一版式

**规划时做一张表**：每页是什么类型、用哪个版式模板、放什么内容。版式库见 [references/layouts.md](references/layouts.md)。

**版式选择原则**：
- 有大数字/指标 → 数据概览版式
- 有时间演进/季度推进 → 时间线或柱状图版式
- 有多个并列要点/挑战 → 卡片拼贴或三栏版式
- 有对比/左右结构 → 左图右文版式
- 有团队/多人/多角色 → 团队页版式
- 封面和结束页各用一次，不重复

### 2. 读设计系统和版式规范

开始写 XML 前，先读：
- [references/design-system.md](references/design-system.md)：配色色值、字体、间距、装饰元素规范
- [references/layouts.md](references/layouts.md)：10 种版式的坐标模板和 XML 片段

**必须用 design-system.md 里的色值，不要自己发明颜色**。

### 3. 逐页制作（一页一闭环）

按飞书 PPT 技能的标准流程：
1. 建空白幻灯片（`slides +create`），拿到 `xml_presentation_id` 和 URL
2. 开工通知用户（present_files 发 URL）
3. 逐页写 XML → 跑 `xml_lint.py`（error 必须为 0）→ `+update-slide` 或 `+add-slide` 写入
4. 全部写完后回读全文，再跑一次 lint，error 必须为 0
5. 截图全部页面，逐页视觉检查
6. 最终 present_files 交付

### 4. XML 写作要点

- 画布固定 960×540
- 中文用「思源黑体」，英文/数字用「Montserrat」
- 标题粗体大字（36–72px），正文 12–16px
- 装饰用几何形状（ellipse、rect、round-rect），不用 emoji
- 文字框宽度要够，`wrap="false"` 配合宽框防止标题换行
- 数字和单位（如"8个"）混排时，单位用 span 并指定 `fontFamily="思源黑体"`，否则 Montserrat 不渲染中文
- 装饰图形（椭圆、色块）不要压住文字框，否则 lint 报 `bbox_overlap` 或 `text_overflows_container`
- 渐变背景必须用 `rgba()` 带百分比停靠点

### 5. 常见 lint 错误速查

| 错误 | 原因 | 修法 |
|---|---|---|
| `text_may_overflow_shape` | 文字框太窄 | 加宽 width，或缩短文字 |
| `bbox_overlap` | 两个填充形状重叠 | 移动位置，或错开层叠 |
| `text_overflows_container` | 文字压住了背景形状 | 把文字移出形状区域，或形状缩小 |
| `shape_out_of_canvas` | 元素超出 960×540 | 调整坐标和尺寸 |
| `text_color_contrast` warning | 浅字压浅底 | 改深字或加深底 |

## 输出要求

- 每页必须有 `<note>` 演讲者备注（3–5 句讲稿）
- 交付前截图全部页面实际查看，不能只跑 lint
- 最终交付飞书 Slides 链接
- 不要在 PPT 里出现 emoji
